[tool:pytest]
addopts =
    --nomigrations
    --ds=enterprise.settings
    --pdbcls=IPython.terminal.debugger:TerminalPdb
python_files =
    test_*.py
env =
    ENV_TYPE=localhost
testpaths =
    tests
filterwarnings =
    ignore::DeprecationWarning
